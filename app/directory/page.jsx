import DirectoryScreen from '@/components/office/DirectoryScreen'

export const metadata = {
  title: 'The building — every floor',
  description: 'Five floors, fifty rooms. Every room is one song with people already inside it.',
}

export default function DirectoryPage() {
  return <DirectoryScreen />
}
